# F1.5 — UI Fiel ao Nokia 3310 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformar a UI genérica da F1 numa réplica fiel do Nokia 3310: boot com logo NOKIA + mãos + startup chime, standby clássico (hora bold topo-direito, "espnokia" centro, WiFi à esquerda, softkey "Menu"), menu carrossel um-item-por-tela com ícones 24×24, fonte nokiafc22 em tudo.

**Architecture:** A lógica do shell (FSM HOME↔LAUNCHER↔APP em `lib/nokiacore`) **não muda** — só as views. Novo app `app_standby` vira o HOME; `menu_view` (carrossel) substitui `launcher_view`; statusbar é removida (o 3310 não tem status bar — cada tela desenha o que precisa). Assets XBM gerados de grids ASCII por `tools/grid2xbm.py` (fonte de verdade em `firmware/assets/assets.txt`). Contrato `App` ganha campo `icon` no fim do struct (não quebra os inicializadores existentes).

**Tech Stack:** PlatformIO (`/home/bernardo/.platformio/penv/bin/pio`, NÃO está no PATH), env `esp32dev` (Arduino core 2.0.x, U8g2 ^2.36.5) e `native` (Unity). Fontes U8g2: `u8g2_font_nokiafc22_tr` (UI geral, conversão oficial da "Nokia Cellphone FC") e `u8g2_font_VCR_OSD_tn` (hora grande do app Relógio). Bold simulado: draw duplo deslocado 1px.

**Contexto do repo:** espnokia-phone — repo público. Trabalho direto no `master`. Specs: `docs/superpowers/specs/2026-06-10-espnokia-phone-design.md` (seção "Identidade").

**Comandos base (sempre destes diretórios):**

```bash
# build firmware
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev
# testes native
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio test -e native
# git (sempre do root)
cd /home/bernardo/projects/espnokia-phone
```

---

## Mapa de arquivos

| Arquivo | Ação | Responsabilidade |
|---|---|---|
| `tools/grid2xbm.py` | já existe (working tree) | converte grids ASCII → header XBM + preview PNG |
| `firmware/assets/assets.txt` | já existe (working tree) | fonte de verdade dos grids (logo, mãos, ícones, wifi) |
| `firmware/src/ui/assets.h` | gerar (Task 1) | arrays XBM + defines W/H (NÃO editar à mão) |
| `firmware/lib/nokiacore/app.h` | modificar (Task 2) | campo `icon` no contrato App |
| `firmware/test/test_shell/main.cpp` | modificar (Task 2) | teste do campo icon |
| `firmware/src/ui/nokia_ui.{h,cpp}` | criar (Task 3) | bold simulado, texto centrado, softkey |
| `firmware/src/drivers/buzzer.{h,cpp}` | modificar (Task 4) | `tune_busy()`; beep não interrompe tune |
| `firmware/src/main.cpp` | modificar (Tasks 4,5,6,7) | beep 1kHz, boot novo, home novo, menu_view |
| `firmware/src/ui/boot_anim.{h,cpp}` | criar (Task 5) | logo NOKIA + mãos convergindo + startup chime |
| `firmware/src/apps/app_standby.{h,cpp}` | criar (Task 6) | tela standby fiel (novo HOME) |
| `firmware/src/ui/menu_view.{h,cpp}` | criar (Task 7) | carrossel um-item-por-tela |
| `firmware/src/ui/launcher_view.{h,cpp}` | **deletar** (Task 7) | substituído pelo menu_view |
| `firmware/src/ui/statusbar.{h,cpp}` | **deletar** (Task 7) | 3310 não tem status bar |
| `firmware/src/ui/icons.h` | **deletar** (Task 7) | substituído por assets.h |
| `firmware/include/version.h` | criar (Task 8) | versão do firmware (tira hardcode do app_about) |
| `firmware/src/apps/app_clock.cpp` | modificar (Task 8) | estilo Clock do 3310: hora grande VCR_OSD |
| `firmware/src/apps/app_tones.cpp` | modificar (Task 8) | header 3310 + toggle Tocar/Parar + ícone |
| `firmware/src/apps/app_about.cpp` | modificar (Task 8) | header 3310 + version.h + ícone |

**Convenção:** ícones de app são **sempre 24×24** (documentado em `app.h`). O `menu_view` desenha `a->icon` com w=h=24 fixos.

---

### Task 1: Toolchain de assets + assets.h gerado

`tools/grid2xbm.py` e `firmware/assets/assets.txt` **já existem no working tree** (criados e validados visualmente — NÃO recriar, NÃO editar). Esta task gera o header e commita tudo.

**Files:**
- Commit: `tools/grid2xbm.py`, `firmware/assets/assets.txt` (já existem)
- Create (gerado): `firmware/src/ui/assets.h`

- [ ] **Step 1: Gerar o header**

```bash
cd /home/bernardo/projects/espnokia-phone
python3 tools/grid2xbm.py firmware/assets/assets.txt --header firmware/src/ui/assets.h
```

Expected: `7 assets -> firmware/src/ui/assets.h`

- [ ] **Step 2: Verificar símbolos gerados**

```bash
grep -E "^#define|_bits\[\]" firmware/src/ui/assets.h
```

Expected — exatamente estes nomes e medidas:

```
#define NOKIA_LOGO_W 45
#define NOKIA_LOGO_H 8
static const unsigned char nokia_logo_bits[] U8X8_PROGMEM = {
#define HAND_LEFT_W 28
#define HAND_LEFT_H 16
static const unsigned char hand_left_bits[] U8X8_PROGMEM = {
#define HAND_RIGHT_W 28
#define HAND_RIGHT_H 16
static const unsigned char hand_right_bits[] U8X8_PROGMEM = {
#define ICON_CLOCK_W 24
#define ICON_CLOCK_H 24
static const unsigned char icon_clock_bits[] U8X8_PROGMEM = {
#define ICON_TONES_W 24
#define ICON_TONES_H 24
static const unsigned char icon_tones_bits[] U8X8_PROGMEM = {
#define ICON_ABOUT_W 24
#define ICON_ABOUT_H 24
static const unsigned char icon_about_bits[] U8X8_PROGMEM = {
#define ICON_WIFI_W 9
#define ICON_WIFI_H 8
static const unsigned char icon_wifi_bits[] U8X8_PROGMEM = {
```

- [ ] **Step 3: Build (header novo não pode quebrar nada)**

```bash
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev
```

Expected: `SUCCESS`

- [ ] **Step 4: Commit**

```bash
cd /home/bernardo/projects/espnokia-phone
git add tools/grid2xbm.py firmware/assets/assets.txt firmware/src/ui/assets.h
git commit -m "feat(assets): toolchain grid2xbm e assets XBM da UI 3310"
```

---

### Task 2: Campo `icon` no contrato App

**Files:**
- Modify: `firmware/lib/nokiacore/app.h`
- Test: `firmware/test/test_shell/main.cpp`

- [ ] **Step 1: Escrever o teste que falha**

Em `firmware/test/test_shell/main.cpp`, fazer 3 edits:

(a) logo após a linha `static bool f_input(Button, BtnEvent) { return consume_input; }`, adicionar:

```cpp
static const unsigned char fake_icon[] = {0x01};
```

(b) trocar a linha

```cpp
static const App a2 = {"Sobre", f_enter, nullptr, nullptr, f_exit, nullptr};
```

por

```cpp
static const App a2 = {"Sobre", f_enter, nullptr, nullptr, f_exit, nullptr, fake_icon};
```

(c) antes de `int main()`, adicionar o teste, e registrar dentro do `main()`:

```cpp
void test_app_exposes_icon() {
  Shell s = make();
  TEST_ASSERT_NULL(s.app_at(0)->icon);                  // a1 sem icon → nullptr
  TEST_ASSERT_EQUAL_PTR(fake_icon, s.app_at(1)->icon);  // a2 com icon
}
```

```cpp
  RUN_TEST(test_app_exposes_icon);
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio test -e native -f test_shell
```

Expected: FAIL — erro de compilação (`App` não tem membro `icon`).

- [ ] **Step 3: Adicionar o campo (no FIM do struct — não quebra inicializadores posicionais)**

`firmware/lib/nokiacore/app.h` fica assim (arquivo completo):

```cpp
#pragma once
#include <stdint.h>
#include "btnlogic.h"

// Contrato de app do NokiaOS. on_render recebe void* para manter o core puro
// (o glue passa U8G2*). Callbacks podem ser nullptr.
// icon: XBM 24x24 (convencao fixa do menu) ou nullptr; fica por ultimo para
// que apps sem icone nem precisem declara-lo (value-init = nullptr).
struct App {
  const char* name;
  void (*on_enter)();
  void (*on_tick)(uint32_t now_ms);
  bool (*on_input)(Button b, BtnEvent e);  // true = consumiu o evento
  void (*on_exit)();
  void (*on_render)(void* gfx);
  const unsigned char* icon;
};
```

- [ ] **Step 4: Rodar todos os testes native**

```bash
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio test -e native
```

Expected: PASS — 19 testes (18 anteriores + `test_app_exposes_icon`).

- [ ] **Step 5: Build esp32dev (apps existentes com 6 inicializadores seguem válidos)**

```bash
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev
```

Expected: `SUCCESS`

- [ ] **Step 6: Commit**

```bash
cd /home/bernardo/projects/espnokia-phone
git add firmware/lib/nokiacore/app.h firmware/test/test_shell/main.cpp
git commit -m "feat(core): campo icon no contrato App"
```

---

### Task 3: Helper `nokia_ui` (bold simulado, centro, softkey)

Idioma visual do 3310: textos de destaque em bold (a fonte original do 3310 é bold; nokiafc22 é regular, então simulamos com draw duplo deslocado 1px) e softkey label centralizado na base.

**Files:**
- Create: `firmware/src/ui/nokia_ui.h`
- Create: `firmware/src/ui/nokia_ui.cpp`

- [ ] **Step 1: Criar `firmware/src/ui/nokia_ui.h`**

```cpp
#pragma once
#include <U8g2lib.h>

// Idioma visual do 3310: bold simulado (draw duplo deslocado 1px) e
// softkey label centralizado na base da tela.
namespace nokia_ui {
// bold simulado na fonte corrente; (x, y) = baseline, como drawStr
void text_bold(U8G2& g, int x, int y, const char* s);
// bold centrado horizontalmente (tela de 84px); y = baseline
void text_bold_center(U8G2& g, int y, const char* s);
// label de softkey centrado na base (sempre nokiafc22 bold, baseline 47)
void softkey(U8G2& g, const char* label);
}  // namespace nokia_ui
```

- [ ] **Step 2: Criar `firmware/src/ui/nokia_ui.cpp`**

```cpp
#include "nokia_ui.h"

namespace nokia_ui {
void text_bold(U8G2& g, int x, int y, const char* s) {
  g.drawStr(x, y, s);
  g.drawStr(x + 1, y, s);
}
void text_bold_center(U8G2& g, int y, const char* s) {
  text_bold(g, 42 - ((int)g.getStrWidth(s) + 1) / 2, y, s);
}
void softkey(U8G2& g, const char* label) {
  g.setFont(u8g2_font_nokiafc22_tr);
  text_bold_center(g, 47, label);
}
}  // namespace nokia_ui
```

- [ ] **Step 3: Build**

```bash
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev
```

Expected: `SUCCESS`

- [ ] **Step 4: Commit**

```bash
cd /home/bernardo/projects/espnokia-phone
git add firmware/src/ui/nokia_ui.h firmware/src/ui/nokia_ui.cpp
git commit -m "feat(ui): helpers nokia_ui (bold simulado, centro, softkey)"
```

---

### Task 4: Buzzer — `tune_busy()` + beep de tecla não interrompe tune + beep DCT3

O piezo só toca um tom por vez (fiel ao 3310). Hoje `beep()` mata o tune em andamento (quirk registrado na F1) e `busy()` não distingue beep de tune — o que quebraria o toggle Tocar/Parar do app Toques (Task 8). Fix: beep vira no-op durante tune; novo `tune_busy()` expõe só o estado do RTTTL. Aproveita e afina o beep de tecla pro padrão DCT3 (~1 kHz, curto).

**Files:**
- Modify: `firmware/src/drivers/buzzer.h`
- Modify: `firmware/src/drivers/buzzer.cpp`
- Modify: `firmware/src/main.cpp` (linha do beep)

- [ ] **Step 1: Atualizar `firmware/src/drivers/buzzer.h`** (arquivo completo)

```cpp
#pragma once
#include <stdint.h>

namespace buzzer {
void init();
void beep(uint16_t freq_hz, uint16_t dur_ms);  // bip de tecla; mudo se tune tocando
void play(const char* rtttl);                  // toca tune, não-bloqueante
void stop();
bool busy();       // qualquer som (beep ou tune)
bool tune_busy();  // só tune RTTTL em andamento
void tick(uint32_t now_ms);                    // chamar todo loop()
}
```

- [ ] **Step 2: Atualizar `firmware/src/drivers/buzzer.cpp`** — 2 edits:

(a) trocar

```cpp
void beep(uint16_t f, uint16_t ms) { playing_ = false; tone_for(f, ms, ms, millis()); }
```

por

```cpp
// um piezo, um canal: o tune tem prioridade (fiel ao 3310 — tecla muda durante toque)
void beep(uint16_t f, uint16_t ms) {
  if (playing_) return;
  tone_for(f, ms, ms, millis());
}
```

(b) logo após a linha `bool busy() { return playing_ || tone_on_; }`, adicionar:

```cpp
bool tune_busy() { return playing_; }
```

- [ ] **Step 3: Afinar o beep de tecla no `firmware/src/main.cpp`**

Trocar

```cpp
    if (e == EV_PRESS) buzzer::beep(1250, 30);  // keypad click
```

por

```cpp
    if (e == EV_PRESS) buzzer::beep(1000, 60);  // keypad beep estilo DCT3 (ajuste fino na bancada)
```

- [ ] **Step 4: Build + testes native (rtttl intacto)**

```bash
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio test -e native
```

Expected: `SUCCESS` + 19 testes PASS.

- [ ] **Step 5: Commit**

```bash
cd /home/bernardo/projects/espnokia-phone
git add firmware/src/drivers/buzzer.h firmware/src/drivers/buzzer.cpp firmware/src/main.cpp
git commit -m "feat(buzzer): tune_busy e beep de tecla sem interromper tune"
```

---

### Task 5: Boot — logo NOKIA + mãos convergindo + startup chime

Substitui o `splash()` do main. Sequência: logo NOKIA no topo, mãos entram pelas laterais e convergem em ~0.9s (estilo "Connecting People"), **startup chime** tocando junto — o jingle de entrada dos Nokia posteriores (arpejo ascendente de Lá maior terminando em A sustentado; aproximação monofônica, ajuste fino de ouvido na bancada). O Nokia tune completo NÃO toca no boot — ele é ringtone do app Toques. Segura a tela até o fim do chime (timeout 4s). Bloqueante de propósito — é a tela de boot, e o `buzzer::tick` roda dentro do loop de espera (mesmo padrão da F1).

**Files:**
- Create: `firmware/src/ui/boot_anim.h`
- Create: `firmware/src/ui/boot_anim.cpp`
- Modify: `firmware/src/main.cpp`

- [ ] **Step 1: Criar `firmware/src/ui/boot_anim.h`**

```cpp
#pragma once
#include <U8g2lib.h>

// Boot estilo Nokia: logo NOKIA + maos convergindo + startup chime.
// Bloqueante (segura ate o fim do chime, timeout 4s); roda buzzer::tick por dentro.
void boot_anim_play(U8G2& g);
```

- [ ] **Step 2: Criar `firmware/src/ui/boot_anim.cpp`**

```cpp
#include "boot_anim.h"
#include <Arduino.h>
#include "assets.h"
#include "drivers/buzzer.h"

// startup chime dos Nokia com as maos: arpejo ascendente de La maior
// terminando em A sustentado (aproximacao monofonica — afinar na bancada)
static const char* kStartupChime =
    "Startup:d=8,o=5,b=140:e,a,c#6,e6,2a6";

// espera dur_ms mantendo o chime andando
static void wait_ticking(uint32_t dur_ms) {
  uint32_t t0 = millis();
  while (millis() - t0 < dur_ms) { buzzer::tick(millis()); delay(2); }
}

void boot_anim_play(U8G2& g) {
  buzzer::play(kStartupChime);
  // maos entram pelas laterais e convergem; a de cima (right) com dedos pra
  // esquerda, a de baixo (left) com dedos pra direita, pontas se cruzando
  const int kFrames = 12;
  const int lx0 = -HAND_LEFT_W, lx1 = 18;   // mao de baixo: entra pela esquerda
  const int rx0 = 84, rx1 = 38;             // mao de cima: entra pela direita
  for (int f = 0; f <= kFrames; f++) {
    g.clearBuffer();
    g.drawXBMP(19, 6, NOKIA_LOGO_W, NOKIA_LOGO_H, nokia_logo_bits);
    g.drawXBMP(rx0 + (rx1 - rx0) * f / kFrames, 18, HAND_RIGHT_W, HAND_RIGHT_H,
               hand_right_bits);
    g.drawXBMP(lx0 + (lx1 - lx0) * f / kFrames, 30, HAND_LEFT_W, HAND_LEFT_H,
               hand_left_bits);
    g.sendBuffer();
    wait_ticking(70);
  }
  // segura logo + maos juntas ate o chime acabar
  uint32_t t0 = millis();
  while (buzzer::busy() && millis() - t0 < 4000) { buzzer::tick(millis()); delay(2); }
}
```

- [ ] **Step 3: Usar no `firmware/src/main.cpp`** — 4 edits:

(a) trocar o include

```cpp
#include "ui/statusbar.h"
```

por

```cpp
#include "ui/statusbar.h"
#include "ui/boot_anim.h"
```

(b) **remover** a função `splash()` inteira (do `static void splash() {` até seu `}`).

(c) **remover** a definição do tune do main (o Nokia tune segue existindo SÓ no app Toques como ringtone):

```cpp
static const char* kNokiaTune =
    "Nokia:d=4,o=5,b=225:8e6,8d6,f#,g#,8c#6,8b,d,e,8b,8a,c#,e,2a";
```

(d) no `setup()`, trocar

```cpp
  splash();
  buzzer::play(kNokiaTune);          // não-bloqueante: splash fica até o fim
  uint32_t t0 = millis();
  while (buzzer::busy() && millis() - t0 < 4000) { buzzer::tick(millis()); delay(2); }
```

por

```cpp
  boot_anim_play(u8g2);              // logo NOKIA + maos + startup chime (segura ate o fim)
```

- [ ] **Step 4: Build**

```bash
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev
```

Expected: `SUCCESS`

- [ ] **Step 5: Commit**

```bash
cd /home/bernardo/projects/espnokia-phone
git add firmware/src/ui/boot_anim.h firmware/src/ui/boot_anim.cpp firmware/src/main.cpp
git commit -m "feat(boot): animacao NOKIA + maos com startup chime"
```

---

### Task 6: `app_standby` — standby fiel como novo HOME

Réplica do standby do 3310: hora bold no topo direito, "espnokia" bold no centro (lugar da operadora), coluna de sinal na esquerda (ícone WiFi no topo + barras procedurais — nível fixo 0 até a F2 ligar o WiFi), **sem bateria**, softkey "Menu" na base. `app_clock` sai do posto de home e entra na lista de apps.

**Files:**
- Create: `firmware/src/apps/app_standby.h`
- Create: `firmware/src/apps/app_standby.cpp`
- Modify: `firmware/src/main.cpp`

- [ ] **Step 1: Criar `firmware/src/apps/app_standby.h`**

```cpp
#pragma once
#include "app.h"

extern const App app_standby;
```

- [ ] **Step 2: Criar `firmware/src/apps/app_standby.cpp`**

```cpp
#include "app_standby.h"
#include <Arduino.h>
#include <U8g2lib.h>
#include "clockfmt.h"
#include "ui/assets.h"
#include "ui/nokia_ui.h"

// nivel de sinal 0..4 — fixo em 0 ate a F2 ligar o WiFi (vira RSSI real)
static uint8_t wifi_level() { return 0; }

static void render(void* gfx) {
  U8G2& g = *(U8G2*)gfx;
  char hhmm[6]; bool colon;
  clock_format(millis(), hhmm, &colon);
  if (!colon) hhmm[2] = ' ';

  g.setFont(u8g2_font_nokiafc22_tr);
  // hora bold no topo direito (como no 3310)
  nokia_ui::text_bold(g, 82 - (int)g.getStrWidth(hhmm), 8, hhmm);
  // "espnokia" bold no centro — lugar do nome da operadora
  nokia_ui::text_bold_center(g, 26, "espnokia");
  // coluna de sinal na esquerda: icone WiFi no topo, barras abaixo
  g.drawXBMP(0, 0, ICON_WIFI_W, ICON_WIFI_H, icon_wifi_bits);
  for (uint8_t i = 0; i < wifi_level(); i++)            // i=0 e a barra de baixo
    g.drawBox(0, 38 - i * 7, 3 + i, 5);                 // largura cresce pra cima
  nokia_ui::softkey(g, "Menu");
}

const App app_standby = {"Standby", nullptr, nullptr, nullptr, nullptr, render, nullptr};
```

- [ ] **Step 3: Trocar o home no `firmware/src/main.cpp`** — 4 edits:

(a) adicionar o include (junto dos outros apps):

```cpp
#include "apps/app_standby.h"
```

(b) trocar

```cpp
static const App* kApps[] = {&app_tones, &app_about};
```

por

```cpp
static const App* kApps[] = {&app_clock, &app_tones, &app_about};
```

(c) trocar

```cpp
  shell.init(&app_clock, kApps, sizeof(kApps) / sizeof(kApps[0]));
```

por

```cpp
  shell.init(&app_standby, kApps, sizeof(kApps) / sizeof(kApps[0]));
```

(d) no `loop()`, o standby agora desenha a própria hora — trocar

```cpp
    char hhmm[6]; bool colon;
    clock_format(now, hhmm, &colon);
    if (!colon) hhmm[2] = ' ';

    u8g2.clearBuffer();
    statusbar_draw(u8g2, hhmm, false);          // wifi off na F1
```

por

```cpp
    u8g2.clearBuffer();
```

e **remover** os includes que sobraram órfãos no topo do main:

```cpp
#include "clockfmt.h"
#include "ui/statusbar.h"
```

- [ ] **Step 4: Build**

```bash
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev
```

Expected: `SUCCESS` (statusbar.cpp ainda compila no projeto, mas ninguém a chama — ela morre na Task 7).

- [ ] **Step 5: Commit**

```bash
cd /home/bernardo/projects/espnokia-phone
git add firmware/src/apps/app_standby.h firmware/src/apps/app_standby.cpp firmware/src/main.cpp
git commit -m "feat(ui): standby fiel ao 3310 como home"
```

---

### Task 7: `menu_view` — carrossel um-item-por-tela + remoção das views antigas

Menu do 3310: nome do app bold no header, ícone 24×24 no centro, scrollbar proporcional na lateral direita, softkey "Selecionar". Um item por tela; UP/DOWN navegam com wrap (o shell já faz isso — view só desenha o item sob o cursor).

**Files:**
- Create: `firmware/src/ui/menu_view.h`
- Create: `firmware/src/ui/menu_view.cpp`
- Modify: `firmware/src/main.cpp`
- Delete: `firmware/src/ui/launcher_view.h`, `firmware/src/ui/launcher_view.cpp`, `firmware/src/ui/statusbar.h`, `firmware/src/ui/statusbar.cpp`, `firmware/src/ui/icons.h`

- [ ] **Step 1: Criar `firmware/src/ui/menu_view.h`**

```cpp
#pragma once
#include <U8g2lib.h>
#include "shell.h"

// Menu do 3310: carrossel um-item-por-tela (header + icone 24x24 central +
// scrollbar proporcional na direita + softkey "Selecionar").
void menu_view_draw(U8G2& g, const Shell& shell);
```

- [ ] **Step 2: Criar `firmware/src/ui/menu_view.cpp`**

```cpp
#include "menu_view.h"
#include "nokia_ui.h"

void menu_view_draw(U8G2& g, const Shell& shell) {
  const App* a = shell.app_at(shell.cursor());
  g.setFont(u8g2_font_nokiafc22_tr);
  // header: nome do app bold no topo
  nokia_ui::text_bold_center(g, 8, a->name);
  // icone 24x24 central (area util desconta a scrollbar a direita)
  if (a->icon) g.drawXBMP(28, 11, 24, 24, a->icon);
  // scrollbar proporcional na lateral direita (trilho + thumb)
  const int track_y = 11, track_h = 26;
  g.drawVLine(82, track_y, track_h);
  int n = shell.app_count();
  int th = track_h / (n > 0 ? n : 1);
  if (th < 6) th = 6;
  int ty = (n > 1) ? track_y + (track_h - th) * shell.cursor() / (n - 1) : track_y;
  g.drawBox(81, ty, 3, th);
  nokia_ui::softkey(g, "Selecionar");
}
```

- [ ] **Step 3: Trocar no `firmware/src/main.cpp`** — 2 edits:

(a) trocar o include

```cpp
#include "ui/launcher_view.h"
```

por

```cpp
#include "ui/menu_view.h"
```

(b) no `loop()`, trocar

```cpp
    if (shell.screen() == Shell::LAUNCHER) launcher_view_draw(u8g2, shell);
```

por

```cpp
    if (shell.screen() == Shell::LAUNCHER) menu_view_draw(u8g2, shell);
```

- [ ] **Step 4: Deletar as views antigas**

```bash
cd /home/bernardo/projects/espnokia-phone
git rm firmware/src/ui/launcher_view.h firmware/src/ui/launcher_view.cpp \
       firmware/src/ui/statusbar.h firmware/src/ui/statusbar.cpp \
       firmware/src/ui/icons.h
```

- [ ] **Step 5: Build**

```bash
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev
```

Expected: `SUCCESS`

- [ ] **Step 6: Commit**

```bash
cd /home/bernardo/projects/espnokia-phone
git add firmware/src/ui/menu_view.h firmware/src/ui/menu_view.cpp firmware/src/main.cpp
git commit -m "feat(ui): menu carrossel um-item-por-tela do 3310"
```

---

### Task 8: Apps no padrão 3310 (Relógio, Toques, Sobre) + version.h

Cada app ganha ícone, fonte nokiafc22 e o idioma do 3310 (header bold + softkey). O Relógio vira a tela Clock do 3310: hora grande (VCR_OSD). O Toques ganha toggle Tocar/Parar usando o `tune_busy()` da Task 4. O Sobre perde a versão hardcoded (issue da F1) — entra `include/version.h`.

**Files:**
- Create: `firmware/include/version.h`
- Modify: `firmware/src/apps/app_clock.cpp`
- Modify: `firmware/src/apps/app_tones.cpp`
- Modify: `firmware/src/apps/app_about.cpp`

- [ ] **Step 1: Criar `firmware/include/version.h`**

```cpp
#pragma once
#define ESPNOKIA_FW_VERSION "0.2.0"  // F1.5 — UI fiel ao 3310
```

- [ ] **Step 2: Reescrever `firmware/src/apps/app_clock.cpp`** (arquivo completo)

```cpp
#include "app_clock.h"
#include <Arduino.h>
#include <U8g2lib.h>
#include "clockfmt.h"
#include "ui/assets.h"
#include "ui/nokia_ui.h"

// Estilo Clock (Menu 11) do 3310: hora grande no centro.
// Futuro: alarme/cronometro. A hora pequena fica no standby, como no original.
static void render(void* gfx) {
  U8G2& g = *(U8G2*)gfx;
  char hhmm[6]; bool colon;
  clock_format(millis(), hhmm, &colon);
  if (!colon) hhmm[2] = ' ';
  g.setFont(u8g2_font_VCR_OSD_tn);
  g.drawStr(42 - (int)g.getStrWidth(hhmm) / 2, 30, hhmm);
  nokia_ui::softkey(g, "Voltar");
}
const App app_clock = {"Relogio", nullptr, nullptr, nullptr, nullptr, render,
                       icon_clock_bits};
```

- [ ] **Step 3: Reescrever `firmware/src/apps/app_tones.cpp`** (arquivo completo)

```cpp
#include "app_tones.h"
#include <U8g2lib.h>
#include "drivers/buzzer.h"
#include "ui/assets.h"
#include "ui/nokia_ui.h"

struct Tone { const char* name; const char* rtttl; };
static const Tone kTones[] = {
    {"Nokia", "Nokia:d=4,o=5,b=225:8e6,8d6,f#,g#,8c#6,8b,d,e,8b,8a,c#,e,2a"},
    {"Kuckuck", "Kuckuck:d=4,o=5,b=125:8g,8e,8g,8e,8g,8e,8c6,8a,8g,8e,8f,8d,2c"},
    {"Sweep", "Sweep:d=16,o=5,b=140:c,e,g,c6,g,e,c"},
};
static const uint8_t kCount = sizeof(kTones) / sizeof(kTones[0]);
static uint8_t cur = 0;

static bool input(Button b, BtnEvent e) {
  if (e != EV_PRESS) return false;
  if (b == BTN_UP) { cur = (cur + kCount - 1) % kCount; return true; }
  if (b == BTN_DOWN) { cur = (cur + 1) % kCount; return true; }
  if (b == BTN_OK) {  // toggle: tocando para, parado toca (tune_busy ignora o beep de tecla)
    if (buzzer::tune_busy()) buzzer::stop();
    else buzzer::play(kTones[cur].rtttl);
    return true;
  }
  return false;  // C nao consumido → shell volta pro menu
}
static void on_exit() { buzzer::stop(); }
static void render(void* gfx) {
  U8G2& g = *(U8G2*)gfx;
  g.setFont(u8g2_font_nokiafc22_tr);
  nokia_ui::text_bold_center(g, 8, "Toques");
  for (uint8_t i = 0; i < kCount; i++) {            // lista com barra invertida (3310)
    int y = 11 + i * 9;
    if (i == cur) {
      g.drawBox(0, y, 84, 9);
      g.setDrawColor(0);
      g.drawStr(3, y + 7, kTones[i].name);
      g.setDrawColor(1);
    } else {
      g.drawStr(3, y + 7, kTones[i].name);
    }
  }
  nokia_ui::softkey(g, buzzer::tune_busy() ? "Parar" : "Tocar");
}
const App app_tones = {"Toques", nullptr, nullptr, input, on_exit, render,
                       icon_tones_bits};
```

- [ ] **Step 4: Reescrever `firmware/src/apps/app_about.cpp`** (arquivo completo)

```cpp
#include "app_about.h"
#include <Arduino.h>
#include <U8g2lib.h>
#include "version.h"
#include "ui/assets.h"
#include "ui/nokia_ui.h"

static void render(void* gfx) {
  U8G2& g = *(U8G2*)gfx;
  char buf[24];
  g.setFont(u8g2_font_nokiafc22_tr);
  nokia_ui::text_bold_center(g, 8, "Sobre");
  g.drawStr(2, 19, "espnokia-phone");
  snprintf(buf, sizeof(buf), "fw %s", ESPNOKIA_FW_VERSION);
  g.drawStr(2, 28, buf);
  snprintf(buf, sizeof(buf), "heap %u KB", (unsigned)(ESP.getFreeHeap() / 1024));
  g.drawStr(2, 37, buf);
  nokia_ui::softkey(g, "Voltar");
}
const App app_about = {"Sobre", nullptr, nullptr, nullptr, nullptr, render,
                       icon_about_bits};
```

(Nota: o "up N s" da F1 sai — a linha de versão entra no lugar e a tela do 3310 não acumula linhas; uptime volta se fizer falta no debug.)

- [ ] **Step 5: Build + testes native**

```bash
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio test -e native
```

Expected: `SUCCESS` + 19 testes PASS.

- [ ] **Step 6: Commit**

```bash
cd /home/bernardo/projects/espnokia-phone
git add firmware/include/version.h firmware/src/apps/app_clock.cpp \
        firmware/src/apps/app_tones.cpp firmware/src/apps/app_about.cpp
git commit -m "feat(apps): apps no padrao 3310 (relogio grande, toques, sobre)"
```

---

### Task 9: Verificação final

- [ ] **Step 1: Build limpo + todos os testes**

```bash
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev -t clean && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev
cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio test -e native
```

Expected: `SUCCESS` + 19 testes PASS.

- [ ] **Step 2: Conferir que nada antigo sobrou**

```bash
cd /home/bernardo/projects/espnokia-phone
grep -rn "launcher_view\|statusbar\|icons.h\|profont" firmware/src/ && echo "SOBROU LIXO" || echo "limpo"
```

Expected: `limpo` (nenhuma referência às views antigas nem às fontes profont).

- [ ] **Step 3: Checklist de bancada (manual — usuário flasha)**

Flash: `cd /home/bernardo/projects/espnokia-phone/firmware && /home/bernardo/.platformio/penv/bin/pio run -e esp32dev -t upload`

| # | Verificação |
|---|---|
| 1 | Boot: logo NOKIA no topo, mãos entram pelas laterais e convergem, startup chime ascendente toca junto (NÃO é o Nokia tune — esse fica no app Toques) |
| 2 | Standby: hora bold topo-direito (com `:` piscando), "espnokia" bold centro, ícone WiFi topo-esquerda, "Menu" na base, **sem bateria** |
| 3 | OK no standby → menu carrossel: "Relogio" + ícone relógio + scrollbar à direita + "Selecionar" |
| 4 | DOWN avança (Toques → Sobre → wrap Relogio), UP retrocede; scrollbar acompanha |
| 5 | Relógio: hora grande estilo VCR centrada, "Voltar" na base; C volta pro menu |
| 6 | Toques: header "Toques" bold, lista com barra invertida, OK toca (softkey vira "Parar"), OK de novo para; teclas mudas durante o tune; C volta e o tune para |
| 7 | Sobre: header bold, fw 0.2.0, heap; C volta |
| 8 | Beep de tecla ~1 kHz/60 ms em toda navegação (ajuste fino de ouvido: editar freq/dur no main.cpp) |
| 9 | C no standby não faz nada; navegação inteira sem travar ~20fps |

---

## Self-review (feito na escrita)

- **Cobertura da spec (seção Identidade):** boot logo+mãos+tune (T5), standby fiel sem bateria (T6), carrossel com scrollbar (T7), submenu com barra invertida (T8/app_tones), ícones originais 24×24 (T1/T8), nokiafc22 + bold simulado (T3, usado em T6-T8), Relógio estilo Clock (T8), keypad beep DCT3 (T4), RTTTL/buzzer (já existia, T4 melhora). ✓
- **Sem placeholders:** todo step de código mostra o código completo. ✓
- **Consistência de tipos:** `App` com 7 campos — `app_standby`/`app_clock`/`app_tones`/`app_about` inicializam todos; `nokia_ui::{text_bold,text_bold_center,softkey}` usados conforme assinaturas da T3; `buzzer::tune_busy()` da T4 usado na T8; símbolos de `assets.h` conferidos na T1 Step 2. ✓
